# Fit Journey — רשימת משימות / פיצ'רים עתידיים

---

## 🤝 חלוקת הכנסה בין בכיר לסגן — ממתין לבחירת עיצוב

בכיר וסגן מלווים לקוח יחד; הבכיר קובע אחוז, וההכנסה מתחלקת. ארבעה
עיצובים ב-`שולחן העבודה/חלוקת-הכנסה-סגנים.pdf`. **טרם נבחר עיצוב, טרם נכתב קוד.**

**ההכרעות שהתקבלו (23/08/2026):**
1. **הסגן רואה הכול** — מחיר החבילה ואיך הוא מתחלק, לא רק את חלקו.
   כלומר `DEPUTY_HIDDEN` נפתח עבור הלקוחות המשותפים בלבד.
2. **החלק מחושב ממה ששולם בפועל**, לא ממחיר החבילה. גבייה חלקית →
   חלק יחסי. המקור הוא `receipts`.
3. **האחוז נשמר פר עסקה** — הוא נחתם על הקבלה ברגע שנוצרה, ושינוי
   אחוז בהמשך אינו משנה את מה שכבר נצבר.
4. **ביטול קבלה מוריד את חלק הסגן; קבלה חדשה מעלה אותו שוב.**
   `receipts.cancelled_at` כבר קיים ומשמש לכך.

**המשמעות למימוש:** אין צורך לחשב מיתרות או ממחירים. כל שורת קבלה
נושאת את האחוז שלה, והסכום הוא סכימה של הקבלות הפעילות. ביטול הוא
פשוט סינון — לא תיקון למפרע.

**נשען על מה שקיים:** `coach_deputies`, `receipts` (כולל ביטולים),
`client_packages` לצפי. אותו עיקרון כמו עמלות הפרזנטורים.

---

---

## 🍎 ממתין למאק (build) — 18/08/2026

תיקוני ההרשאות נדחפו לריפו האפליקציה אבל **לא נכנסים דרך `sync-web`** —
הם בקבצים נייטיביים ודורשים בנייה מחדש. עד אז מצלמה ומיקרופון עדיין
סוגרים את האפליקציה בלחיצה.

- [ ] **`sync-web.js` במאק** — למשוך את תיקוני הצ'אט מריפו האתר ל-`www`.
      (ב-Windows הוא נופל: `rsync` לא קיים שם. במאק צריך גם
      `WEB_SRC` אם תיקיית האתר לא נקראת `fit-journey`.)
- [ ] **בנייה מחדש** — iOS + Android. בלי build חדש שינויי
      `Info.plist` / `AndroidManifest.xml` פשוט לא קיימים במכשיר.
- [ ] **להעלות `CURRENT_PROJECT_VERSION`** לפני העלאה לאפל — בניה עם
      אותו מספר נדחית ב-App Store Connect.
- [ ] **לבדוק על מכשיר אמיתי, לא בסימולטור:**
      - מתאמן → צ'אט → 📎 → נפתח בורר שמציג **סרטונים**, לא רק תמונות.
      - תיעוד סט → מצלמה נפתחת ו**האפליקציה לא נסגרת**.
      - 🎙️ אצל שני הצדדים → בקשת הרשאה למיקרופון מופיעה פעם אחת.
      - סרטון של ~40MB באמת מגיע לצד השני.

**למה זה קרה:** iOS מקריס אפליקציה שניגשת למצלמה/מיקרופון בלי מחרוזת
הסבר ב-`Info.plist`. שלוש המחרוזות נוספו (`NSCamera`, `NSMicrophone`,
`NSPhotoLibrary`), ובאנדרואיד `RECORD_AUDIO` + `CAMERA`. זה גם היה
כשל ודאי בבדיקת אפל.

---

## ⏳ ממתין לך בסופאבייס (לא קשור למאק) — עודכן 20/08/2026

### נותר לפרוס
שתי הפונקציות **פרוסות ועובדות**, אבל הקוד שלהן השתנה אחרי הפריסה
האחרונה. עד שייפרסו מחדש, שתי היכולות האלה פשוט לא קיימות בשרת:

- [ ] `admin` — נערכה 16/08 (סיסמת סגן). בלי פריסה מחדש קביעת סיסמה
      לסגן נכשלת ב"המתאמן אינו שייך אליך".
- [ ] `telegram-shop` — נערכה 15/08 (פרזנטורים + עמלה מהרווח).

- [ ] **`TG_OWNER_ID = 773162451` ב-Secrets** — הדיווח על כל הזמנה
      לחשבון הפרטי כבר כתוב בקוד, כולל שם, שם משתמש, כתובת, אמצעי
      תשלום, עמלה וכפתור שיחה עם הלקוח. בלי הסוד `OWNER_ID` ריק
      והשליחה מדולגת בשקט — זו הסיבה היחידה שלא הגיעו הודעות.

### החלטות פתוחות
- [ ] האם לחסום את `admin.html` מחבילת האפליקציה.
- [ ] **תקרת האחסון 50MB** — נמדדה מול השרת (52,428,800 בתים עוברים,
      בית אחד מעל נדחה). אם תרצה סרטונים ארוכים יותר אפשר להעלות את
      `file_size_limit` של הדלי `client-files`. **אם תעשה זאת צריך
      לעדכן גם את הקוד** (`CAP` ב-`dashboard.html`, `COACH_FILE_CAP`
      ב-`coach.html`) — אחרת הבדיקה בדפדפן תמשיך לחסום ב-50 גם כשהשרת
      כבר מרשה יותר.

### ✅ הורצו
מיגרציות: `admin_delete_notifications`, `coach_exercises`,
`coach_deputies`, `coach_foods`, `daily_cardio`, `cardio_seconds`.
כולן אומתו חי מול ה-DB, כולל בדיקות שליליות שמוודאות ש-RLS דוחה
כתיבה לא מורשית.

---

## ✅ סוכן 05 (תוכנית לפי תמונות) — הושלם 07/2026
נבנה במלואו: Edge Function `photo-plan` (ניתוח אנתרופומטרי דטרמיניסטי + ראייה + ולידציה קשיחה), טבלת `body_photos` + bucket פרטי `body-photos`, UI במאמן (טופס תמונות+מדדים), תמחור 500/750 מ-token_usage, ואלבום התקדמות. פירוט האפיון המקורי למטה (לתיעוד).

---

## 📄 אפיון סוכן 05 (לתיעוד — כבר מומש)

### סוכן AI שבונה תוכנית לפי תמונות גוף (סריקת גוף)
**מטרה:** הסוכן שבונה תוכניות אימון יוכל לקבל **תמונות גוף** של המתאמן, "לסרוק" אותן, ולבנות תוכנית מותאמת אישית.

**מה הסוכן צריך לעשות (ברמת הרעיון — האפיון המלא יגיע מהמשתמש):**
- לקבל תמונות גוף של המתאמן (כמה זוויות?).
- לזהות **נקודות חלשות וחזקות** — אילו שרירים מפותחים ואילו מפגרים.
- לפי זה להרכיב:
  - **נפח שבועי לכל שריר** (sets/week per muscle) — יותר לנקודות החלשות.
  - **חלוקת אימונים** (split) שמתאימה לפרופיל.

**תמחור (הוחלט):** 500 טוקנים ל-5 התוכניות הראשונות (per coach), ואחר כך 750 טוקנים לכל תוכנית. (צריך לעקוב אחרי מספר תוכניות התמונות שהמאמן בנה — למשל ספירה מ-token_usage עם kind='photo_plan', או counter על coaches.)

**סטטוס:** לא התחיל. המשתמש אמר: "שיהיה זמן נשב על שאסביר לך באפיון שלם איך צריכה להראות הסריקה."

**שאלות פתוחות לאפיון (לשאול את המשתמש כשנשב על זה):**
- כמה תמונות / אילו זוויות (קדמי/גב/צד, מכווץ/רפוי)?
- איך מזינים את התמונות — בממשק המאמן? בממשק הלקוח?
- האם הניתוח דרך מודל ראייה (Claude vision) — צריך להעביר את התמונות דרך ה-Edge Function `ai` (שכבר תומך ב-multimodal אם מוסיפים image blocks).
- איך מגדירים "נקודה חלשה" — יחסית לשאר הגוף? לפי סטנדרט?
- פורמט הפלט — אותו JSON של תוכנית אימון הקיים, עם נפח מותאם.
- פרטיות: תמונות גוף רגישות — איפה נשמרות (bucket `client-files`?), מי רואה.

---

## ✅ הושלם לאחרונה (לתיעוד)
- מאמן אוטומטי: מודל שעות (10/שעה + 10 עמלת הפעלה), תגובה תוך ~30 שניות, יומן שיחות, הכשרת תזונה.
- באדג' הודעות שלא-נקראו ליד שם המתאמן בסיידבר.
- אבטחה: הסרת מפתחות SERVICE_ROLE + Anthropic מהדפדפן, מעבר ל-Edge Functions + RLS.
- תפקידי מאמנים (regular=צפייה), היסטוריית טוקנים, דרופ סט, אימון פעיל שנשמר.
